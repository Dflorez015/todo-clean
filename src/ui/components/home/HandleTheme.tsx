import { Button } from "@style/comon-styled"
import { HandleThemeWrapper } from "@style/home-styled"

export const HandleTheme = () => {
    return (
        <HandleThemeWrapper>
            <Button type="button"> themes </Button>
        </HandleThemeWrapper>
    )
}
